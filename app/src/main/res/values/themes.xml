<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecycloMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RecycloMate" parent="Base.Theme.RecycloMate" />

    //Login signup input outer box style
    <style name="LoginTextInputOuterFieldStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:layout_marginBottom">15dp</item>
    </style>

    <!-- Text color-->
    <style name="txtcolo">
        <item name="android:textColor">@color/black</item>
    </style>
    <!-- Button style light mode -->
    <style name="BUTTON">
        <item name="android:background">@drawable/rounded_button_bg</item>
    </style>

    <style name="TEXT">
        <item name="android:textColor">@color/black</item>
    </style>

</resources>